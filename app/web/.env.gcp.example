# ==========================================
# GCP Production Env Template (Web)
# File: app/web/.env
# ==========================================

# Public URL that browser uses to call API.
# Staging (path-based):
NEXT_PUBLIC_API_BASE_URL=http://34.124.244.233/baligirls/api

# If API is routed by same domain via NGINX (production recommendation), example:
# NEXT_PUBLIC_API_BASE_URL=https://REPLACE_WEB_DOMAIN/api

# Base path when app is hosted under a path prefix (example: /baligirls)
NEXT_PUBLIC_BASE_PATH=/baligirls
NEXT_BASE_PATH=/baligirls

# Upload storage (server-side, used by /api/upload and /api/uploads/*)
GCS_BUCKET_NAME=gda-s01-bucket
GCS_UPLOAD_PREFIX=baligirls/uploads

# Staging credential file path (temporary staging approach)
GOOGLE_APPLICATION_CREDENTIALS=/var/www/baligirls/.secrets/gda-s01-storage-key.json
